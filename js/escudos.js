/* ============================================================
   escudos.js — Motor gráfico de escudos IA (0 KB, CSS puro)
   FASE 1: los 6 escudos humanos ya están pintados a mano en
   index.html (.escudo--rayas / .escudo--rombo) como demo visual.
   FASE 2 (pendiente):
     - Leer data/equipos_ia.json y, por cada equipo, generar un
       nodo `.escudo` con las custom properties --primary /
       --secondary y la clase --rayas o --rombo según
       `escudoFormato`, sin tocar ninguna imagen.
     - Exponer una función pública, p. ej.
       window.Escudos.crear(equipo) -> HTMLElement
       reutilizable tanto por las cajas humanas como por las
       filas de clasificación / plantillas IA.
   ============================================================ */
(function () {
  "use strict";
  // Intencionalmente vacío en Fase 1.
})();
