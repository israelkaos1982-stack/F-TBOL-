/* ============================================================
   main.js — Orquestador de la app
   FASE 1: esqueleto. Aún no hace fetch de ningún data/*.json.
   FASE 2 (pendiente):
     - Sustituir las 6 cajas humanas hardcodeadas en index.html
       por un render dinámico desde data/equipos.json.
     - Wiring del router de columna central (data-view del nav):
       limpiar #main-content con innerHTML = "" antes de redibujar
       cada sección para no acumular memoria (regla anti-OOM).
     - Delegar el click de cada .team-box a js/calendario.js
       (columna derecha) y a la sección de plantilla/estadísticas
       (columna central).
   ============================================================ */
(function () {
  "use strict";
  // Intencionalmente vacío en Fase 1.
})();
